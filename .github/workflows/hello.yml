name: Checkout & Build Pipeline
on: 
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello GitHub Actions"
      - uses: actions/checkout@v4
      - run: ls -la
      - run: echo "Build Started"
